# SPEC_047_CG6 — Final crown package

**Cite as:** **`SPEC_047_CG6`**.

## Purpose

Produce the **terminal integrative** summit: **exact assumptions**, **exact boundaries**, **dependency graph**, and **adversarial-readiness**—so a **hostile** reader **cannot** fairly dismiss the crown as **`geOutput` only**, **`mentionBound` only**, proof-height only, trace-only encoding, or singleton-ladder organization **without** the new bundled predicates.

## Hard boundaries

- **Blocked universals** remain **documentation + boundary theorems** only.
- **No `sorry`.** **No new axioms.**
- **No weakening** earlier theorems.

## Preconditions

**`SPEC_042_CG1`** through **`SPEC_046_CG5`** must meet **acceptance** **or** document **formal blockers** and **rescoped** final theorem with **explicit** limitation lemmas (still **no** fake crown).

## Required new modules

| Module path | Role |
|-------------|------|
| `NoveltyTheory/Summits/FinalCrownPackage.lean` | **Final** named theorems + re-exports + interpretation docstrings |

## Required documentation (same epic closure commit as final Lean)

| Path | Role |
|------|------|
| [`docs/CROWN_DEPENDENCY_MAP.md`](../../../../docs/CROWN_DEPENDENCY_MAP.md) | Theorem DAG: crown **inputs / outputs / boundaries** |
| [`docs/CROWN_POSITIONING.md`](../../../../docs/CROWN_POSITIONING.md) | What crown **claims**, what it **refutes**, what is **out of scope** |
| [`docs/THEOREM_INVENTORY.md`](../../../../docs/THEOREM_INVENTORY.md) | **Every** new `theorem` / `lemma` row |
| [`MANIFEST.md`](../../../../MANIFEST.md) | Module map / axiom policy |
| [`docs/004_LEAN_LIBRARY_LAYOUT.md`](../../../../docs/004_LEAN_LIBRARY_LAYOUT.md) | If **`NoveltyTheory/`** graph changes |

## Required theorem names (minimum)

| Name | Intent |
|------|--------|
| `final_crown_iterated_structural_ascent` | **`∃G ∃Φ ∀n`**, structural cert, **`ProvesAt (n+1)`**, **`¬ ProvesAt n`** |
| `final_crown_no_fixed_structural_stratum` | **No fixed final** stratum (**exact** formal statement ties to org / proof regime—**no** overclaim vs blocked universals) |
| `final_crown_future_defeat_of_terminality` | Links to **StratifiedFinality** / **FutureDefeat** language honestly |
| `final_crown_generator_truth_family` | Bundled **generator-truth** / **structural** family packaging |

## Target summit content (informal)

Align with:

\[
\exists G,\exists(\Phi_n),\forall n,\ 
\Phi_n \text{ structural about } G
\land \mathrm{ProvesAt}(n{+}1,\Phi_n)
\land \neg \mathrm{ProvesAt}(n,\Phi_n).
\]

Plus honest links to:

- **non-final** explanatory / proof-theoretic stratum (scoped),
- **future defeat** of terminality claims (scoped),
- **organization** obstruction (**`SPEC_045`**).

## Summit theorem metadata (mandatory per theorem)

- **Exact Lean name**
- **One-sentence** informal claim
- **Direct dependencies** (prior lemmas/modules)
- **Interpretation boundary** (what universal forms are **not** claimed—cite inventory obstruction lemmas)

## Acceptance test (adversarial)

**Complete** only if **all** collapse questions fail plausibly:

1. Only **`geOutput`** again?
2. Only **`mentionBound`** again?
3. Only current **proof-height** template?
4. Only **nat** encoding again?
5. Only **singleton-ladder org** again?

If **any** “yes” remains **honest**, **iterate** **`042`–`046`** or **narrow** final claims with **explicit** boundary lemmas.

## Dependencies

- **`SPEC_042_CG1`**–**`SPEC_046_CG5`**.

## Status

**Complete** — `Summits.FinalCrownPackage`; crown docs + inventory updated in-repo.
