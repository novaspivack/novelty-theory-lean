# SPEC_040_MD1 — Modalities (scoping and first obligations)

**Cite as:** **`SPEC_040_MD1`**.

## Purpose

Scope **modal** extensions (necessity/possibility over **regimes**, **stages**, or **explanatory worlds**) for later incompleteness-style claims. This spec is explicitly **horizon-setting**: it may end at **definitions + conservative fragments** until `EPIC_009` crown needs interact.

## Acceptance

1. **Formal choices:** Document (in this spec’s body, updated as decisions land) which modality, which **base logic**, and which **frame** properties are **assumed vs proved**.

2. **Lean baseline:** Module skeleton under `NoveltyTheory/` with **no misleading** theorems—either proven lemmas from minimal axioms **disclosed** in [`MANIFEST.md`](../../../../MANIFEST.md), or `TODO` stubs **only** if repository policy forbids merge; preferred path is **proof-carrying** tiny fragment.

3. **Integration note:** Cross-link to **`SPEC_037_CR1`** if crown statements quantify over modalized \(\Phi_n\).

## Dependencies

- **`SPEC_014_ES2`**–**`SPEC_018_UEN2`** as semantic baseline; optional dependency on **`SPEC_038_XS1`** if modalities attach to extended atoms.
