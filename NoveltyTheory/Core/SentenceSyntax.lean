/-!
# Sentence syntax module (`SPEC_013_ES1`)

Roadmap files name **`SentenceSyntax.lean`**; the inductive lives in **`Sentence.lean`** and is
re-exported through this aggregate for stable import paths.
-/

import NoveltyTheory.Core.Sentence
