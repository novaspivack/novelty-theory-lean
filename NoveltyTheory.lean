import NoveltyTheory.All

/-!
# `NoveltyTheory` — Lake root module

Lake’s `lean_lib «NoveltyTheory»` (without explicit `roots`) requires this file at the package root,
same pattern as `UgpLean.lean` in **`ugp-lean`**. The library body lives under `NoveltyTheory/`.
-/
